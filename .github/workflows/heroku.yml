name: Deploy to Heroku

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: a4a93467-30eb-438f-b5cc-56f8306f5d3c
          heroku_app_name: "doggo-dev-chart" #Must be unique in Heroku
          heroku_email: "murata@vt.edu"
          appdir: "doggo-dev-chart"
